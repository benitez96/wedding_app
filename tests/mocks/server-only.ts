/**
 * Mock for server-only package
 *
 * server-only is a Next.js package that throws an error when imported in client code.
 * In tests, we don't need this behavior, so we mock it as an empty module.
 */

export {};
