import HeartLoader from "@/components/HeartLoader";

/**
 * Loading state for the public invitation page.
 * Uses the themed HeartLoader for a consistent experience.
 */
export default function InvitationLoading() {
  return <HeartLoader />;
}
