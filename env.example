# Configuración de la aplicación
NEXT_PUBLIC_WEDDING_DATE=20260214

# Base de datos PostgreSQL
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
POSTGRES_DB=wedding_app
POSTGRES_PORT=5432
POSTGRES_HOST=db
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

# Seguridad - JWT Secret (DEBE ser cambiado en producción)
# Generar con: openssl rand -hex 32
JWT_SECRET=tu-super-secreto-jwt-de-al-menos-32-caracteres-aqui

# Better Auth - Authentication (REQUERIDO)
# Generar secret con: openssl rand -base64 32
BETTER_AUTH_SECRET=tu-secret-de-better-auth-de-al-menos-32-caracteres
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Configuración del entorno
NODE_ENV=production
PORT=3000

# Configuración de seguridad adicional (REQUERIDA en producción)
# Lista de orígenes permitidos para CORS, separados por comas
CORS_ORIGIN=https://tudominio.com,https://www.tudominio.com
RATE_LIMIT_ENABLED=true
SECURITY_HEADERS_ENABLED=true

#GRAFANA
GRAFANA_PASSWORD=admin123

# Dominio
DOMAIN=yourdomain.com

# Basic auth para subdominios protegidos (genera con: caddy hash-password --plaintext TU_PASS)
BASIC_AUTH_HASH=